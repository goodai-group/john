import { BusinessFormData } from '../types';
import { calculateAssessmentReport } from './scoringEngine';

export const SAMPLE_PROJECT: BusinessFormData | null = null;
export const INITIAL_SAMPLE_REPORT: any = null;
