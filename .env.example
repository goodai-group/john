# ============================================================
# 1. Gemini AI (AI 答疑 / 智能分析)
# GEMINI_API_KEY: Gemini AI API 调用密钥。
# AI Studio 会在运行时从用户密钥自动注入；本地开发请到 https://aistudio.google.com/apikey 获取。
# 不配置时，AI 答疑会回退为"本地规则库"模式（仅能回答有限预设问题）。
# ============================================================
GEMINI_API_KEY=""

# ============================================================
# 2. Supabase 云端数据库 + Google 登录（替代 Firebase）
# 步骤：
#   1) 在 https://supabase.com 新建项目
#   2) 打开 Dashboard → Project Settings → API，复制 Project URL 与 anon public key 填入下方
#   3) 打开 SQL Editor，执行 src/lib/supabaseClient.ts 中的 SUPABASE_DATABASE_SCHEMA_SQL 建表脚本
#   4) 打开 Authentication → Providers → Google，启用并填入 Google OAuth Client ID / Secret
#   5) 打开 Authentication → URL Configuration，把站点地址（如 http://localhost:3000）加入 Redirect URLs
#   6) 配置完成后重启开发服务器
#
# 注意：前端构建可读 VITE_* 与 NEXT_PUBLIC_*（vite.config.ts 已配置 envPrefix）；服务端可读 SUPABASE_* / NEXT_PUBLIC_* / VITE_*（依次回退）。
# 建议用同一套真实值配置 VITE_* 与 SUPABASE_*，保证前端与后端都能识别。
# ============================================================
VITE_SUPABASE_URL=""
VITE_SUPABASE_ANON_KEY=""
NEXT_PUBLIC_SUPABASE_URL=""
NEXT_PUBLIC_SUPABASE_ANON_KEY=""
SUPABASE_URL=""
SUPABASE_ANON_KEY=""
